# CET-4 作文批改助手 · 后端版部署说明

## 一、这是什么

在原来纯前端网页基础上，加了**后端激活码校验 + 设备绑定**，实现「一码一设备、防分享」。

## 二、项目结构

```
cet4-grader-server/
├── api/
│   ├── _lib.js          # 核心逻辑（激活码校验、设备绑定）
│   ├── activate.js      # 激活接口
│   └── verify.js        # 验证设备接口
├── public/
│   ├── index.html       # 前端（激活码登录）
│   └── ocr/             # OCR 库
├── 生成激活码.js         # 本地生成激活码
├── 导入激活码.js         # 导入激活码到数据库
├── package.json
└── vercel.json
```

## 三、部署步骤（Vercel）

### 第 1 步：注册 Vercel

1. 打开 https://vercel.com
2. 点 Sign Up，选择 **Continue with GitHub**
3. 用你的 GitHub 账号（snkrg-gpu）登录授权

### 第 2 步：创建 KV 数据库

1. 在 Vercel 后台，进入 **Storage** 标签
2. 点 **Create Database** → 选择 **KV (Redis)**
3. 命名（如 `cet4-kv`），选择免费区域，创建
4. 记下 KV 的连接信息（`KV_REST_API_URL` 和 `KV_REST_API_TOKEN`）

### 第 3 步：部署项目

方式一（命令行，推荐）：
```bash
npm install -g vercel
vercel login
cd cet4-grader-server
vercel --prod
```

方式二（网页导入）：
1. Vercel 后台 → Add New → Project
2. 选择 cet4-grader 仓库（或 Import 这个文件夹）
3. 配置环境变量 KV_REST_API_URL 和 KV_REST_API_TOKEN
4. Deploy

### 第 4 步：生成并导入激活码

```bash
cd cet4-grader-server
node 生成激活码.js 50    # 生成 50 个激活码
node 导入激活码.js        # 导入到 KV 数据库（需先设置环境变量）
```

### 第 5 步：测试

打开部署后的网址，输入一个激活码，应该能激活进入。

## 四、工作原理

1. 买家输入激活码 → 前端采集设备指纹 → 发送到后端
2. 后端校验激活码：未用 → 绑定设备指纹，标记已用；已用 → 检查指纹是否匹配
3. 同设备下次访问 → 前端自动调 verify 接口 → 指纹匹配 → 免激活直接进
4. 换设备 → 指纹不匹配 → 拒绝，提示激活码已在其他设备使用

## 五、激活码管理

- 生成：`node 生成激活码.js [数量]`
- 导入：`node 导入激活码.js`
- 查看已用：在 Vercel KV 后台查看 `code:*` 键

## 六、注意事项

- 设备指纹基于浏览器信息，买家清缓存/换浏览器会导致指纹变化，需要重新激活（这是「平衡模式」的取舍）
- 免费额度：Vercel 免费版 KV 有额度限制，个人小规模销售够用
